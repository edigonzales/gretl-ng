buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://plugins.gradle.org/m2/" }
        maven { url "http://jars.interlis.ch"; allowInsecureProtocol true }
    }
    dependencies {
        classpath "io.github.sogis:gretl-ng:latest.integration"
    }
}

apply plugin: "ch.so.agi.gretl-ng"

import ch.so.agi.gretl.tasks.*

ext {
    foo = "a"
}

task fubar() {
    doLast {
        println foo
        foo = "b"
        println foo
    }
}

task importData() {
    doLast {
        println foo
    }
}

task validateData(type: IliValidator, dependsOn: 'fubar') {
    models = "DM01"
    greeting = foo
    modeldir = foo
    dummy = foo
}